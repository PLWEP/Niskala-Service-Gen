# CLI Dry Run

Safely preview changes before writing them to disk.

## Usage

```bash
dart run niskala_service_gen --dry-run
```

The tool will log all files that would be generated or modified without affecting your filesystem. Use this to verify configuration changes or new metadata impacts.
