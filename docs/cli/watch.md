# CLI Watch Mode

Experience live regeneration as you edit your configuration or metadata.

## Usage

```bash
dart run niskala_service_gen --watch
```

The tool will:

1. Run an initial generation.
2. Watch `niskala.yaml` for changes.
3. Automatically regenerate services on every save.
